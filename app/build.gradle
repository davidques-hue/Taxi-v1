plugins { id 'com.android.application' }

android {
    namespace 'cl.taxicaja.app'
    compileSdk 36

    defaultConfig {
        applicationId 'cl.taxicaja.app'
        minSdk 24
        targetSdk 36
        versionCode 3
        versionName '1.2'
    }
}
